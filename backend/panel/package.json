{
  "name": "truvex-sourcing-admin-panel",
  "version": "0.1.0",
  "private": true,
  "description": "Truvex Sourcing Express/EJS admin panel",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check app.js && node --check server.js && node --check routes/authRoutes.js && node --check routes/dashboardRoutes.js && node --check routes/recordRoutes.js && node --check routes/serviceRoutes.js && node --check routes/categoryRoutes.js",
    "seed:admin": "node scripts/create-admin.js",
    "test": "node --test tests/e2e.test.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.7",
    "ejs": "^3.1.10",
    "express": "^4.21.2",
    "express-session": "^1.18.1",
    "helmet": "^8.0.0",
    "morgan": "^1.10.0",
    "mysql2": "^3.11.5",
    "slugify": "^1.6.6"
  },
  "engines": {
    "node": ">=18"
  }
}
